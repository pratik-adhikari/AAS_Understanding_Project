---
basyx:
  cors:
    allowed-origins: "http://localhost:3000"
    allowed-methods: "GET,POST,PATCH,DELETE,PUT,OPTIONS,HEAD"

spring:
  data:
    mongodb:
      uri: "mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongo:27017/aas-registry?authSource=admin"

