# 01 — The AAS Information Model

This chapter will be completed alongside the executable model. Every term will
be linked to a concrete object in the generated Digital Nameplate.

Required topics:

- identifiers and `idShort`;
- AssetInformation;
- shell-to-submodel references;
- submodel elements;
- semantic IDs;
- Concept Descriptions;
- AASX packaging;
- JSON and XML serialization.

