# 03 — Quickstart

The quickstart will be enabled after the first runnable Compose milestone.

It will contain:

- exact prerequisites;
- environment initialization;
- startup;
- health verification;
- browser entry points;
- teardown and full cleanup.

